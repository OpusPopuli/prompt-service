# Release validation evidence pack — prompt-service v0.4.0

| | |
|---|---|
| **Release** | v0.4.0 (`document-analysis-petition` v2 — classification-first) |
| **Prepared** | 2026-08-23 |
| **Prepared by** | Claude (op-release), operator Rodney Gagnon |
| **Compliance profile** | **None declared.** Framework-agnostic evidence only. |

> **Honesty guardrail:** audit-ready scaffolding and evidence; not by
> itself organizational compliance.

## 1. Scope

| PR | Issue | Title | Data classification |
|---|---|---|---|
| [#108](https://github.com/OpusPopuli/prompt-service/pull/108) | [#103→#107](https://github.com/OpusPopuli/prompt-service/issues/107) (closed) | classification-first petition template | **Privacy-positive.** The template now instructs the model that skip responses use a closed reason enum and must never echo document text — a scanned bogus document can contain personal information. No new data enters this service. |
| [#106](https://github.com/OpusPopuli/prompt-service/pull/106) | — | release.yml `contents: write` for SBOM attach | CI-only |

## 2–3. Traceability & change control

Plan-of-record: opuspopuli `docs/plans/1057-non-petition-gate.md`
(subtask 1) → #107 → #108 → tests → this pack. Same standing gaps as
v0.3.0 (no RTM/change-record scaffolding in this repo; self-review
without countersign — merges performed by Rodney countersigning the PRs).

## 4. Verification & validation

- **IQ-equivalent:** unchanged from v0.3.0 (lockfile, `node:22-alpine`
  Dockerfile, CI). `.nvmrc` gap persists.
- **OQ-equivalent:** 299/299 unit (incl. new seeded-template lockstep
  test: classification block ordered before untrusted text, no unresolved
  placeholders, verbatim skip shapes); 179/179 Docker integration (incl.
  new contract test: both skip shapes, conservative-bias and no-echo
  rules, `promptVersion === "v2"`). Coverage above enforced floors.
- **PQ-equivalent:** behavioral verification against the production model
  (qwen3.5:9b, `think: false` per llm-provider): 4/4 — menu and receipt
  → `skip:not_a_petition`; genuine CA initiative sheet → full analysis;
  deliberately OCR-corrupted petition → full analysis (conservative bias
  held; the launch-critical false-negative case). Consumer-side behavior
  verified in opuspopuli#1058's suites.

## 5. Security

Pre-push gate all green on #108 and #106 (coverage, jscpd, SonarJS,
audit — 1 LOW residual, Trivy, gitleaks, AI code + security reviews).
This release is the first to exercise the #106 permission fix; SBOM
attach expected to succeed automatically.

## 6. Regulated-data handling

No profile declared; strict lens applied. The change *strengthens* data
handling: the no-echo rule prevents scanned-document text (potentially
personal correspondence) from appearing in skip responses that the
consumer persists and caches. Request audit log unchanged
(endpoint/version/key-prefix/region only).

## 7. Risk

| Risk | Mitigation | Status |
|---|---|---|
| Classifier rejects a genuine petition (launch 2026-08-27) | Conservative-bias rule in template; consumer skip-rate metric + forceReanalyze recovery; 4/4 real-model verification incl. corrupted-OCR case | Closed for release; monitor metric post-deploy |
| Template/consumer contract drift | Lockstep tests both repos | Closed |
| Node not re-seeded → classifier dark | Documented in changelog Deploy note + opuspopuli#1058 release notes; behavior degrades to v1 (safe) | Open until deploy verified |

## 8. Exceptions

Carried from v0.3.0: no RTM/change-record scaffolding; no `.nvmrc`;
54 untriaged Dependabot findings on default branch (1 critical, 34 high —
still needs a dedicated pass); v0.2.0 is a record-only tag (no image).
No `--no-verify` pushes or gate waivers in this release.
